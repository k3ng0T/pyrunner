# ⚡ Python Среда Удаленного Выполнения (Web Terminal)

[![Python](https://img.shields.io/badge/Python-3.x-blue.svg)](https://www.python.org/)
[![Flask](https://img.shields.io/badge/Flask-SocketIO-lightgrey.svg)](https://flask-socketio.readthedocs.io/)
[![Architecture](https://img.shields.io/badge/Architecture-Async%20Subprocess-orange.svg)]()

<!-- Language Switcher -->
<div align="center">
  <h3>
    <a href="README.md">🇬🇧 English</a> | 🇷🇺 Русский
  </h3>
</div>

---

## Концепция и Архитектура

Этот проект — не просто подсветка синтаксиса. Это **полностью асинхронный движок удаленного выполнения кода**, который переносит мощь локального терминала в браузер.

Большинство простых веб-компиляторов имеют две критические проблемы:
1.  **Блокировка I/O:** Они зависают при использовании `time.sleep()`.
2.  **Интерактивность:** Они ломаются или вешают сервер при попытке использовать `input()`.

**Этот проект решает обе проблемы.** Он создает мост между веб-интерфейсом и ядром сервера через WebSockets, обеспечивая двустороннюю связь в реальном времени.

### 🛠 Под капотом: Как это работает

Магия заключается в управлении процессами. Это не просто `eval()` кода.

1.  **Изоляция процессов:** При нажатии "Run" сервер порождает отдельный процесс ОС (`subprocess.Popen`). Это гарантирует, что основной сервер останется живым, даже если пользовательский код уйдет в бесконечный цикл.
2.  **Неблокирующие потоки:** Фоновые треды (Threads) непрерывно читают потоки `stdout` и `stderr` запущенного процесса.
3.  **Транспорт в реальном времени:** Как только Python-процесс выдает строку, она попадает в очередь и мгновенно отправляется на фронтенд через **Socket.IO**.
4.  **Интерактивный ввод:** Когда скрипт доходит до `input()`, он ставится на паузу. Сервер продолжает работать. Когда вы вводите данные в браузере, они летят через сокет и инжектируются прямо в `stdin` подпроцесса.

### 🚀 Возможности

*   **Настоящая интерактивность:** Запуск скриптов, требующих ввода данных (`input()`), работает так же, как в консоли.
*   **Live Streaming:** Наблюдайте за выполнением циклов в реальном времени (идеально для скриптов с задержками или прогресс-барами).
*   **Умный редактор:** Написанная с нуля логика для текстового поля, которая обрабатывает отступы (Tab) и автоформатирование (Enter после `:`) по аналогии с VS Code.
*   **Управление процессом:** Полный контроль жизненного цикла. Вы можете экстренно остановить (Kill) выполнение скрипта в любой момент.

### 📥 Установка и Использование
Чтобы запустить проект на своем компьютере, перейдите к инструкции:

👉 **[ЧИТАТЬ ИНСТРУКЦИЮ ПО УСТАНОВКЕ (LOCALHOST.ru.md)](LOCALHOST.ru.md)**